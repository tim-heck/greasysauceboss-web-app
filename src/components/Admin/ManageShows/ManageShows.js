import React, { Component } from 'react';
import { connect } from 'react-redux';

class ManageShows extends Component {

    render() {
        return (
            <>

            </>
        );
    }
}

export default connect()(ManageShows);