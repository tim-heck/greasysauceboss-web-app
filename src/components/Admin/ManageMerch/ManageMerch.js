import React, { Component } from 'react';
import { connect } from 'react-redux';

class ManageMerch extends Component {

    render() {
        return (
            <>

            </>
        );
    }
}

export default connect()(ManageMerch);