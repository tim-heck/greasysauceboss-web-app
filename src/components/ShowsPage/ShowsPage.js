import React, { Component } from 'react';
import { connect } from 'react-redux';

class ShowsPage extends Component {

    render() {
        return (
            <>

            </>
        );
    }
}

export default connect()(ShowsPage);